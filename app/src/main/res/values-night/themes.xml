<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Registro de los colores de cada elemento de la aplicación cuando el dispositivo tenga la configuración en modo oscuro,
         utilizando los registrados en el fichero 'colors.xml'.-->

    <style name="Base.Theme.Proyecto_DAM" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryVariant">@color/colorPrimaryDark</item>
        <item name="colorOnPrimary">@color/black</item>
        <item name="android:windowBackground">@color/windowBackgroundNightColor</item>
        <item name="android:textColor">@color/textDark</item>
        <item name="android:titleTextColor">@color/textDark</item>
        <item name="android:icon">@color/colorIconsButtonsDark</item>
    </style>
</resources>